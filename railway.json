{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "sh -c \"test -f ${DB_PATH:-buyersforce.db} || python3 seed.py; exec gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --threads 4 --timeout 60\""
  }
}
